import React from "react";

const NavbarTwo = () => {
  return (
    <nav style={{
      padding: "1rem",
      background: "#f4f6f8",
      display: "flex",
      justifyContent: "center",
      borderBottom: "2px solid #e0e0e0"
    }}>
      <span style={{ fontWeight: "bold", fontSize: "1.2rem", color: "#555" }}>
        CenterBrand
      </span>
    </nav>
  );
};

export default NavbarTwo;